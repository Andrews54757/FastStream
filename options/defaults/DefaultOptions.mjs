import { DefaultKeybinds } from "./DefaultKeybinds.mjs";

export const DefaultOptions = {
    playMP4URLs: false,
    playStreamURLs: true,
    analyzeVideos: true,
    downloadAll: true,
    autoEnableURLs: [],
    keybinds: DefaultKeybinds
}